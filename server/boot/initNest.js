module.exports = function initNest(app) {
  //console.log('Initializing nestRemoting for models');
// example
//  app.models.RidAgency.nestRemoting('flowtoUser');
  //app.models.Group.nestRemoting('gfiles');
}
